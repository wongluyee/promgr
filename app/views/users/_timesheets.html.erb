<div class="div2 mt-4 mb-0 me-5 p-3 boxes text-light d-flex flex-column justify-content-center align-items-center">
  <h5>Timesheets</h5>
  <div class="clock-in-out-button">
    <% if current_user.timesheets.last.time_in.to_date != @today %>
      <%= render 'timesheets/clock_in' %>
    <% elsif @timesheet.time_out == nil %>
      <%= render 'timesheets/clock_out' %>
    <% else  %>
      <h3> Good work today <%= current_user.name %> 🎉</h3>
    <% end %>
  </div>
</div>
